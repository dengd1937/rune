diff --git a/src/utils.py b/src/utils.py
new file mode 100644
index 0000000..5555555
--- /dev/null
+++ b/src/utils.py
@@ -0,0 +1,9 @@
+def merge_tags(tags, prefix=[]):
+    prefix.append("default")
+    names = []
+    for tag in tags:
+        if tag == None:
+            continue
+        names = names + [tag + prefix]
+    return names
