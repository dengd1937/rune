name: hook-tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install deps
        run: pip install -r requirements-dev.txt
      - name: Run deterministic tests (hooks + skills + reviewers harness)
        run: pytest tests/hooks/ tests/skills/ tests/reviewers/ -v
